// Interfaz para la respuesta de la API (GET)
export interface Miembro {
    id:         number;    
    name:        string;
    lastname:    string;
    phone:       string;
    address:     string;   
}